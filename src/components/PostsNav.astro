---
interface Props {
    current?: "posts" | "archive" | "tags";
}

const { current } = Astro.props;
---

<nav class="gvns-posts-nav" aria-label="Posts section">
    <a href="/posts" class:list={["gvns-posts-nav__link", { active: current === "posts" }]}>Posts</a>
    <a href="/posts/archive" class:list={["gvns-posts-nav__link", { active: current === "archive" }]}>Archive</a>
    <a href="/posts/tags" class:list={["gvns-posts-nav__link", { active: current === "tags" }]}>Tags</a>
    <a href="/rss.xml" class="gvns-posts-nav__link" target="_blank" rel="noopener noreferrer">RSS</a>
</nav>

<style>
    .gvns-posts-nav {
        display: flex;
        gap: 0.5rem;
        flex-wrap: wrap;
        margin-bottom: 1.5rem;
    }

    .gvns-posts-nav__link {
        display: inline-block;
        padding: 0.375rem 0.875rem;
        font-size: var(--text-sm);
        font-weight: 500;
        color: var(--colour-text-secondary);
        background: var(--colour-bg-secondary);
        border-radius: 9999px;
        text-decoration: none;
        transition:
            color var(--transition-fast),
            background-color var(--transition-fast);
    }

    .gvns-posts-nav__link:hover {
        color: var(--colour-accent-primary);
        background: var(--colour-bg-tertiary);
    }

    .gvns-posts-nav__link:focus-visible {
        outline: 2px solid var(--colour-accent-primary);
        outline-offset: 2px;
    }

    .gvns-posts-nav__link.active {
        color: var(--colour-bg-primary);
        background: var(--colour-accent-primary);
    }

    .gvns-posts-nav__link.active:hover {
        color: var(--colour-bg-primary);
        background: var(--colour-accent-hover);
    }
</style>
