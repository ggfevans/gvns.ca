---
import type { CollectionEntry } from 'astro:content';
import TagList from './TagList.astro';
import { safeUrl } from '@utils/format';

interface Props {
  project: CollectionEntry<'work'>;
}

const { project } = Astro.props;
const { title, description, status, tags, repo, url } = project.data;
const safeRepoUrl = safeUrl(repo);
const safeLiveUrl = safeUrl(url);
---

<article class="gvns-work-card">
  <div class="gvns-work-card__header">
    <h3>
      <a href={`/work/${project.slug}`}>{title}</a>
    </h3>
    <span class={`gvns-work-card__status gvns-work-card__status--${status}`}>{status}</span>
  </div>
  <p class="gvns-work-card__description">{description}</p>
  <TagList tags={tags} linked={false} />
  {(safeRepoUrl || safeLiveUrl) && (
    <div class="gvns-work-card__links">
      {safeRepoUrl && <a href={safeRepoUrl} class="gvns-work-card__link" target="_blank" rel="noopener noreferrer">GitHub<span class="gvns-sr-only"> (external)</span></a>}
      {safeLiveUrl && <a href={safeLiveUrl} class="gvns-work-card__link" target="_blank" rel="noopener noreferrer">Live<span class="gvns-sr-only"> (external)</span></a>}
    </div>
  )}
</article>

<style>
  .gvns-work-card {
    background: var(--colour-bg-secondary);
    border: 1px solid var(--colour-border);
    border-left: var(--border-accent) solid var(--colour-p1-violet);
    border-radius: var(--radius-xl);
    padding: var(--space-6);
    transition: box-shadow var(--transition-base), border-color var(--transition-fast);
  }

  .gvns-work-card:hover {
    border-color: var(--colour-accent-primary);
    box-shadow: 0 10px 15px -3px rgb(139 92 246 / 0.05);
  }

  .gvns-work-card__header {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    gap: 1rem;
    margin-bottom: 0.5rem;
  }

  h3 {
    font-size: var(--text-xl);
    font-weight: 600;
    margin: 0;
  }

  h3 a {
    color: var(--colour-text-primary);
    text-decoration: none;
    transition: color var(--transition-fast);
  }

  h3 a:hover {
    color: var(--colour-accent-primary);
  }

  .gvns-work-card__description {
    color: var(--colour-text-secondary);
    font-size: var(--text-sm);
    line-height: 1.6;
    margin-bottom: 1rem;
  }

  .gvns-work-card__status {
    font-size: var(--text-xs);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    padding: 0.25rem 0.5rem;
    border-radius: 999px;
    background: var(--colour-bg-tertiary);
    color: var(--colour-text-secondary);
    white-space: nowrap;
  }

  .gvns-work-card__status--active,
  .gvns-work-card__status--maintained {
    color: var(--colour-accent-primary);
  }

  .gvns-work-card__status--archived {
    color: var(--colour-text-muted);
  }

  .gvns-sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border-width: 0;
  }

  .gvns-work-card__links {
    display: flex;
    gap: 1rem;
    margin-top: 1rem;
  }

  .gvns-work-card__link {
    color: var(--colour-accent-primary);
    text-decoration: none;
    font-size: var(--text-sm);
    transition: color var(--transition-fast);
  }

  .gvns-work-card__link:hover {
    color: var(--colour-accent-hover);
  }
</style>
